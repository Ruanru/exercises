//Create a function that receives an array of numbers
//and returns an array containing only the positive numbers



var allNumbers = [2,7, 9, 2, 0, 89, 08]
check = 0
function check(allNumbers){
    for(var count = 0; count < allNumbers.length; count++){
        var check = count[allNumbers]
        var even = count[allNumbers]
            if(check % 2 == 0){
                even = check[allNumbers]
            }else{break}
        }
        return even
}
var retorno = even
console.log(even)